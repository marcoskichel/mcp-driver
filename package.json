{
  "name": "mcp-driver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "vitest",
    "build": "esbuild src/index.ts --bundle --outdir=dist --platform=node --target=es2020 --format=esm --sourcemap",
    "build:watch": "esbuild src/index.ts --bundle --outdir=dist --platform=node --target=es2020 --format=esm --sourcemap --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/marcoskichel/mcp-driver.git"
  },
  "keywords": [],
  "author": "",
  "license": "GPL-2.0-only",
  "bugs": {
    "url": "https://github.com/marcoskichel/mcp-driver/issues"
  },
  "homepage": "https://github.com/marcoskichel/mcp-driver#readme",
  "devDependencies": {
    "esbuild": "^0.25.2",
    "rimraf": "^6.0.1",
    "vitest": "^3.1.1"
  }
}
